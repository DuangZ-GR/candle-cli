{
  "benchmark_id": "torch2ms-scanner-v1",
  "description": "Curated syntax coverage for deterministic PyTorch API discovery.",
  "cases": [
    {"id":"direct-001","source":"import torch\ntorch.abs(x)\n","expected_apis":["torch.abs"]},
    {"id":"direct-002","source":"import torch\ntorch.add(x, y)\n","expected_apis":["torch.add"]},
    {"id":"direct-003","source":"import torch\ntorch.arange(10)\n","expected_apis":["torch.arange"]},
    {"id":"direct-004","source":"import torch\ntorch.cat([x, y])\n","expected_apis":["torch.cat"]},
    {"id":"direct-005","source":"import torch\ntorch.einsum('ij,jk->ik', x, y)\n","expected_apis":["torch.einsum"]},
    {"id":"direct-006","source":"import torch\ntorch.flatten(x)\n","expected_apis":["torch.flatten"]},
    {"id":"direct-007","source":"import torch\ntorch.gather(x, 0, index)\n","expected_apis":["torch.gather"]},
    {"id":"direct-008","source":"import torch\ntorch.matmul(x, y)\n","expected_apis":["torch.matmul"]},
    {"id":"direct-009","source":"import torch\ntorch.mean(x)\n","expected_apis":["torch.mean"]},
    {"id":"direct-010","source":"import torch\ntorch.ones(2, 3)\n","expected_apis":["torch.ones"]},
    {"id":"direct-011","source":"import torch\ntorch.rand(2, 3)\n","expected_apis":["torch.rand"]},
    {"id":"direct-012","source":"import torch\ntorch.randn(2, 3)\n","expected_apis":["torch.randn"]},
    {"id":"direct-013","source":"import torch\ntorch.reshape(x, (3, 2))\n","expected_apis":["torch.reshape"]},
    {"id":"direct-014","source":"import torch\ntorch.softmax(x, dim=1)\n","expected_apis":["torch.softmax"]},
    {"id":"direct-015","source":"import torch\ntorch.split(x, 2)\n","expected_apis":["torch.split"]},
    {"id":"direct-016","source":"import torch\ntorch.stack([x, y])\n","expected_apis":["torch.stack"]},
    {"id":"direct-017","source":"import torch\ntorch.sum(x, dim=1)\n","expected_apis":["torch.sum"]},
    {"id":"direct-018","source":"import torch\ntorch.tensor([1, 2])\n","expected_apis":["torch.tensor"]},
    {"id":"direct-019","source":"import torch\ntorch.transpose(x, 0, 1)\n","expected_apis":["torch.transpose"]},
    {"id":"direct-020","source":"import torch\ntorch.zeros(2, 3)\n","expected_apis":["torch.zeros"]},
    {"id":"alias-001","source":"import torch as t\nt.sum(x)\n","expected_apis":["torch.sum"]},
    {"id":"alias-002","source":"import torch.nn.functional as F\nF.relu(x)\n","expected_apis":["torch.nn.functional.relu"]},
    {"id":"alias-003","source":"from torch import sum\nsum(x)\n","expected_apis":["torch.sum"]},
    {"id":"alias-004","source":"from torch import sum as tsum\ntsum(x)\n","expected_apis":["torch.sum"]},
    {"id":"alias-005","source":"from torch import nn\nnn.Linear(2, 3)\n","expected_apis":["torch.nn.Linear"]},
    {"id":"alias-006","source":"from torch.nn import functional as F\nF.gelu(x)\n","expected_apis":["torch.nn.functional.gelu"]},
    {"id":"alias-007","source":"from torch.nn.functional import relu as activate\nactivate(x)\n","expected_apis":["torch.nn.functional.relu"]},
    {"id":"alias-008","source":"from torch import Tensor as T\nT([1])\n","expected_apis":["torch.Tensor"]},
    {"id":"alias-009","source":"import torch.linalg as la\nla.norm(x)\n","expected_apis":["torch.linalg.norm"]},
    {"id":"alias-010","source":"import torch.special as special\nspecial.expit(x)\n","expected_apis":["torch.special.expit"]},
    {"id":"method-001","source":"import torch\nx = torch.tensor([1.0])\nx.reshape(1, 1)\n","expected_apis":["torch.tensor","torch.Tensor.reshape"]},
    {"id":"method-002","source":"import torch\nx = torch.tensor([1.0])\nx.sum()\n","expected_apis":["torch.tensor","torch.Tensor.sum"]},
    {"id":"method-003","source":"import torch\nx = torch.tensor([1.0])\nx.float()\n","expected_apis":["torch.tensor","torch.Tensor.float"]},
    {"id":"method-004","source":"import torch\nx = torch.tensor([1.0])\nx.bool()\n","expected_apis":["torch.tensor","torch.Tensor.bool"]},
    {"id":"method-005","source":"import torch\nx = torch.tensor([1.0])\nx.permute(0)\n","expected_apis":["torch.tensor","torch.Tensor.permute"]},
    {"id":"method-006","source":"import torch\nx = torch.tensor([1.0])\nx.unsqueeze(0)\n","expected_apis":["torch.tensor","torch.Tensor.unsqueeze"]},
    {"id":"method-007","source":"import torch\nx = torch.tensor([1.0])\nx.detach()\n","expected_apis":["torch.tensor","torch.Tensor.detach"]},
    {"id":"method-008","source":"import torch\ndef run(x: torch.Tensor):\n    return x.transpose(0, 1)\n","expected_apis":["torch.Tensor.transpose"]},
    {"id":"method-009","source":"import torch.nn.functional as F\ny = F.relu(x)\ny.mean()\n","expected_apis":["torch.nn.functional.relu","torch.Tensor.mean"]},
    {"id":"method-010","source":"import torch\ntorch.rand(2, 3).flatten()\n","expected_apis":["torch.Tensor.flatten","torch.rand"]},
    {"id":"negative-001","source":"import numpy as np\nnp.sum(x)\n","expected_apis":[]},
    {"id":"negative-002","source":"items = []\nitems.append(1)\n","expected_apis":[]},
    {"id":"negative-003","source":"text = 'x'\ntext.split()\n","expected_apis":[]},
    {"id":"negative-004","source":"def sum(x): return x\nsum(1)\n","expected_apis":[]},
    {"id":"negative-005","source":"from other import torch\ntorch.sum(x)\n","expected_apis":[]},
    {"id":"negative-006","source":"import tensorflow as torch\ntorch.sum(x)\n","expected_apis":[]},
    {"id":"negative-007","source":"obj.reshape(2, 3)\n","expected_apis":[]},
    {"id":"negative-008","source":"getattr(other, 'sum')(x)\n","expected_apis":[]},
    {"id":"negative-009","source":"import torch as t\nt = object()\nt.sum(x)\n","expected_apis":[]},
    {"id":"negative-010","source":"import torch as t\ndef run(t):\n    return t.sum(x)\n","expected_apis":[]}
  ]
}
